#!/bin/bash

STARTPWD="$(pwd)"
cd -- "$(dirname "$(readlink -f "$0")")/.." || exit 1
BASEDIR="$(pwd)"
cd -- "$STARTPWD" || exit 1

NCIBLES_LIBDIR="$BASEDIR/lib"
. "$BASEDIR/lib/core.lib.sh"

REQUIRE ncibles

ncibles "$@"
