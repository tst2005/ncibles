#!/bin/bash

STARTPWD="$(pwd)"
cd -- "$(dirname "$0")/.." || exit 1
BASEDIR="$(pwd)"
cd -- "$STARTPWD" || exit 1

NCIBLES_LIBDIR="$BASEDIR/lib"
. "$BASEDIR/lib/core.lib.sh"

REQUIRE cible

uplevel_ssh_use_master=''
uplevel_ssh_interactive=''
cible "$@"
